Extract canonical requirement frames from this controlled 6-question requirements dialogue.

Return JSON only. Do not include markdown fences or explanations.

Output rules:
- Use the provided JSON schema exactly.
- Normalize noisy user wording into concise canonical phrasing.
- Do not invent unsupported details.
- Every frame must have status = "confirmed".
- Use one frame for system context from turn 2.
- Use one frame per user role from turn 4.
- Use one frame per functional capability from turn 6.
- Use one frame per authentication method from turn 8.
- If authentication is explicitly not required, return one authentication frame with constraint = "not_required" and object = null.
- Use one frame per performance constraint from turn 10.
- Use one frame per security constraint from turn 12.

Field conventions:
- system_context:
  - actor = null
  - action = "system_type"
  - object = canonical system type
- user_role:
  - actor = canonical role
  - action = "user_role"
- functional_capability:
  - actor = canonical actor
  - action = canonical capability phrasing
- authentication:
  - action = "authenticate"
  - object = authentication method if present
  - constraint = "required" or "not_required"
- performance/security:
  - constraint = canonical constraint text

Sample id: {{SAMPLE_ID}}
Domain hint: {{DOMAIN}}

Dialogue:
{{DIALOGUE}}
